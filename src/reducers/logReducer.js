const intialState ={
    logs:null,
    current:null,
    loading:false,
    error:null
}

const logReducer = (state=intialState,action) => {
  switch (action.type) {
  
    default:
        return state    
  }
  
  
  
    
}

export default logReducer